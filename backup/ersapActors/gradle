#!/bin/bash

# Simple wrapper script for gradle

# Check if gradle is installed
if ! command -v gradle &> /dev/null; then
    echo "Gradle is not installed. Please install it first."
    exit 1
fi

# Run gradle with the provided arguments
gradle "$@" 