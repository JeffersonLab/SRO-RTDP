[scheduler]
    allow implicit tasks = True
    UTC mode = True

[scheduling]
    [[graph]]
        R1 = """
            sender => gpu_proxy => receiver
        """

[runtime]
    [[root]]
        script = """
            # Load necessary modules
            module load apptainer
        """
        [[[environment]]]
            # Network configuration
            RECEIVER_PORT = 50080
            GPU_PROXY_PORT = 50888
            # Container configuration
            DOCKER_SOURCE = "jlabtsai/rtdp-gpu_proxy:latest"
            SIF_FILE = "gpu-proxy.sif"

    [[sender]]
        inherit = root
        platform = jlab_slurm
        execution time limit = PT2H
        [[[directives]]]
            --partition = ifarm
            --ntasks = 1
            --cpus-per-task = 4
            --mem = 8G
        script = """
            # Run sender application
            apptainer exec ${CYLC_WORKFLOW_RUN_DIR}/sifs/${SIF_FILE} \
                python3 /app/sender.py --port ${GPU_PROXY_PORT}
        """

    [[gpu_proxy]]
        inherit = root
        platform = jlab_slurm
        execution time limit = PT2H
        [[[directives]]]
            --partition = gpu
            --ntasks = 1
            --cpus-per-task = 4
            --mem = 16G
            --gres = gpu:1
        script = """
            # Run GPU proxy application
            apptainer exec --nv ${CYLC_WORKFLOW_RUN_DIR}/sifs/${SIF_FILE} \
                ./gpuEmu --port ${GPU_PROXY_PORT}
        """

    [[receiver]]
        inherit = root
        platform = jlab_slurm
        execution time limit = PT2H
        [[[directives]]]
            --partition = ifarm
            --ntasks = 1
            --cpus-per-task = 4
            --mem = 8G
        script = """
            # Run receiver application
            apptainer exec ${CYLC_WORKFLOW_RUN_DIR}/sifs/${SIF_FILE} \
                python3 /app/receiver.py --port ${RECEIVER_PORT}
        """ 